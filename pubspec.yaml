name: bbtotal
description: Flutter app for preparing clock-in data before rebuilding the workflow.
publish_to: 'none'
version: 1.0.1+2

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.1.5
  geolocator: ^13.0.0
  http: ^1.3.0
  permission_handler: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
